# Digest Fetchers

Data fetcher modules for the morning briefing agent.

Each fetcher should export an async function that returns structured data for one project or data source.

## Available Fetchers
- (none yet — add project-specific fetchers here)

## Convention
Each fetcher file should be named after the data source it fetches from (e.g. `github.js`, `vercel.js`, `instagram.js`).
