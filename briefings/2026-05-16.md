# Morning Briefing — Saturday, May 16, 2026

## Top 3 Priorities Today

1. **Update trending data in `TrendingSection.jsx`** — the footer still reads "Last update: March 2026". It's now May. The trend data itself is 2+ months stale. Refresh signals or update the copy.
2. **Add Bio Generator and Content Calendar tools** — both are listed as "Coming Soon" in `ToolsPreview.jsx` and `SofarContent.jsx`. These are the most obvious growth gaps on the live site.
3. **Set `VITE_OPENROUTER_API_KEY` in Vercel env vars** — the entire AI cascade fails silently if this key is missing. Verify it's set in the Vercel dashboard for the production deployment.

---

## Project: SofarContent (sofarcam.vercel.app)

**Status: Live and functional**

The site is deployed on Vercel. Core stack: React 19 + Vite 7 + Tailwind 4 + Framer Motion. All serverless functions live in `/api/`.

### Live tools (working)
- Concept Generator — 3 viral concepts via `generate-content.js` + Anthropic API
- Hook Writer — ranked hooks with scores and medals
- Caption Writer — 5 platform-ready captions
- LinkedIn Writer — 3 scored posts with profile tip
- Content Analyzer — YouTube/Twitter fetch + AI breakdown

### Coming Soon (blocking growth)
- Bio Generator (`status: 'soon'` in `ToolsPreview.jsx`)
- Content Calendar (`status: 'soon'` in `ToolsPreview.jsx`)

### AI infrastructure
`llmFetch.js` runs an 11-model cascade via OpenRouter:
- Tier 1 (free): Trinity Large, Gemini 2.5 Flash, Llama 3.3 70B, DeepSeek V3, Gemini 2.0 Flash, Kimi K2
- Tier 2 (paid, cheap): DeepSeek V3.2, GPT-4.1 Mini, Gemini Flash Pro
- Tier 3 (fallback): Claude Haiku, Grok 3 Mini

The Concept Generator also has a direct Anthropic path via `/api/generate-content.js` using `ANTHROPIC_API_KEY`. Both routes need their env vars set in Vercel.

### Email list
`/api/subscribe.js` uses Resend. Requires `RESEND_API_KEY` and `RESEND_AUDIENCE_ID` env vars. New subscriber notifications go to `camcurrence@gmail.com`. This is wired and deployed.

### Trending section
`TrendingSection.jsx` has hardcoded trend data for 6 platforms (Instagram, TikTok, YouTube, Pinterest, YouTube Shorts, Etsy). Footer says "Last update: March 2026" — needs refresh.

---

## What Each Area Needs Next

| Area | Next Action |
|------|-------------|
| Coming Soon tools | Build Bio Generator and Content Calendar components |
| Trending data | Update `TrendingSection.jsx` with fresh platform signals, fix date copy |
| Custom domain | Buy `sofar.cam` or `sofarcam.com` and connect in Vercel (DEPLOY.md has steps) |
| Email list | Check Resend audience dashboard for subscriber count |
| Platforms | `SofarContent.jsx` supports Instagram, TikTok, YouTube, YouTube Shorts, Pinterest, Etsy — LinkedIn only in the LinkedIn Writer tab, not in the main concept generator |

---

## Blockers

- **No CLAUDE.md exists** — project has no high-level documentation file. Context has to be reconstructed from source files each session.
- **Trend data is stale** — hardcoded March 2026 data shipped as "updated weekly." Either automate or manually refresh on a real schedule.
- **`digest/src/fetchers/` does not exist** — briefing agent spec referenced this path, but this repo has no digest pipeline. All data is either hardcoded or fetched live by the frontend.
- **OpenRouter referrer is `sofar-cam.vercel.app`** in `llmFetch.js` — verify this matches the actual Vercel deployment URL (check if it's `sofarcam.vercel.app` vs `sofar-cam.vercel.app`).

---

## Recent Work (last 5 commits)

- `a3a99e1` Wire email capture to real Resend API
- `7302320` Fix Upwork profile URL to real slug
- `b814c90` Replace placeholder Upwork/Discord URLs with real links
- `329f5fe` Session memory + chained workflow for all tools, Trending in navbar
- `a4ba20b` Add 5 competitive features: session memory, chained workflow, viral score bar, trending section, email capture
