# Morning Briefing — Thursday, May 21, 2026

---

## Top 3 Priorities Today

1. **Buy sofar.cam domain** — site is live on Vercel but still on `sofarcam.vercel.app`. The custom domain is the last step to making this feel real. Porkbun has `.cam` for ~$10–20/yr.
2. **Add real Upwork/Discord URLs to Links.jsx** — `DEPLOY.md` flags these as placeholders. If those links are live, update them. If not, decide what to put there.
3. **Update the Trending section** — last update note says "March 2026." It's May. Either manually update the trend data in `TrendingSection.jsx` or build a weekly update workflow so it stays credible.

---

## Project Status

### SofarContent Hub Site (`sofarcam.vercel.app`)

**Status: Live but needs polish**

All five tools are live and wired:
- Concept Generator — calls Claude Sonnet via `/api/generate-content.js`
- Hook Writer, Caption Writer, LinkedIn Writer — all wired to `llmFetch` (OpenRouter multi-model cascade)
- Content Analyzer — pulls YouTube oEmbed + fxtwitter, no API key required

Email capture is wired to Resend. Subscriber notification fires to `camcurrence@gmail.com` on each signup.

Session memory and chained workflow (concept → hooks → captions) is working via `sessionStore.js`.

**What it needs next:**
- Custom domain (`sofar.cam` or `sofarcam.com`)
- Two "Coming Soon" tools to go live: Bio Generator and Content Calendar — these are placeholders in `ToolsPreview.jsx` with no backend yet
- Trending section data is stale (says March 2026) — update or automate
- `vercel.json` is present — confirm env vars `ANTHROPIC_API_KEY`, `VITE_OPENROUTER_API_KEY`, `RESEND_API_KEY`, and `RESEND_AUDIENCE_ID` are set in Vercel dashboard

### Shot by Seven Photography Brand

**Status: Referenced but not built here**

This repo is the SofarContent/sofar.cam hub. Shot by Seven (`shotbyseven.com`) is a separate site. No code for it lives here.

**What it needs next:**
- Nothing in this repo — separate project
- Cross-link from this site's footer/links is present (`@shotbyseven777` Instagram reference in `ToolsPreview.jsx`)

### Content Pipeline

**Status: No content tracked yet**

`CLAUDE.md` defines a workflow: ideas → `content/IDEAS.md`, status → `content/PIPELINE.md`. Neither file exists. No content has been staged or tracked in this repo.

**What it needs next:**
- Decide if you want to use this repo for content tracking or keep it code-only
- If yes, create `content/IDEAS.md` and `content/PIPELINE.md` and start logging

---

## Blockers

- **Domain not purchased** — site is not on a real URL yet. This is the highest-visibility gap.
- **Trending data is stale** — "March 2026" timestamp visible to users on a live site. Erodes trust.
- **Bio Generator + Content Calendar are placeholders** — users who click "Coming Soon" cards get nothing. Set a ship date or pull them from the grid until ready.
- **No content/IDEAS.md or PIPELINE.md** — if you're planning to use Claude for content drafting, the workflow infrastructure doesn't exist yet.

---

## Notes

- Stack: React 19 + Vite 7 + Tailwind 4 + Framer Motion 12 + Three.js (3D hero scene)
- AI backend: Anthropic Claude Sonnet 4.6 (paid services tool) + OpenRouter 11-model cascade (free tools)
- Email: Resend (audience list + owner notification on signup)
- Deployment: Vercel (serverless functions in `/api/`)
- Last commit: `feat: wire email capture to real Resend API`
