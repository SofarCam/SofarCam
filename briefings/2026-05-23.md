# Morning Briefing — Saturday, May 23, 2026

---

## Top 3 Priorities Today

1. **Add Bio Generator tool** — it's listed as "Coming Soon" in ToolsPreview and is the next logical step after Concept Generator + Hook Writer + Caption Writer are live. Builds the toolkit toward feature parity.
2. **Update TrendingSection "Last update" timestamp** — currently says "March 2026", which is 2 months stale. Kills credibility of the "What's Working Right Now" section.
3. **Verify Resend env vars are set in Vercel** — the email capture API has a silent fallback (`fallback: true`) if `RESEND_API_KEY` is unset. Subscribers could be silently lost without confirmation.

---

## Project: sofar.cam (sofarcam)

**Status: Active / Live on Vercel**

The site is a single-page React + Vite app deployed to `sofarcam.vercel.app`. Stack: React 19, Tailwind 4, Framer Motion 12, Three.js for the hero scene.

### What's Live
- **5 AI tools** in SofarContent: Concept Generator, Hook Writer, Caption Writer, LinkedIn Writer, Content Analyzer
- **TrendingSection** with platform tabs (Instagram, TikTok, YouTube, Pinterest, YouTube Shorts, Etsy) and momentum scores
- **EmailCapture** wired to Resend API for waitlist signups + owner notification emails
- **Links section** with real URLs: shotbyseven.com, @sofar.cam, @shotbyseven777, YouTube, Upwork, SofarSeven Discord
- **Multi-model AI cascade** (11 models via OpenRouter, 6 free-tier first, paid fallbacks)
- **Session memory + chained workflow**: concepts flow into Hook Writer or Caption Writer automatically

### Last 5 Commits
| Commit | Change |
|--------|--------|
| `a3a99e1` | Wire email capture to real Resend API |
| `7302320` | Fix Upwork profile URL to real slug |
| `b814c90` | Replace placeholder Upwork and Discord URLs |
| `329f5fe` | Session memory + chained workflow for all tools |
| `a4ba20b` | Add viral score bar, trending section, email capture |

---

## What Each Area Needs Next

### Tools (SofarContent)
- **Bio Generator** — already teased as "Coming Soon" (`status: 'soon'` in ToolsPreview). UI slot exists, needs implementation.
- **Content Calendar** — second "Coming Soon" tool in the preview grid. 30-day posting plan generator.
- **TrendingSection data freshness** — hardcoded trend data says "March 2026". Either update the copy or pull from a real API. At minimum, update the date string in `TrendingSection.jsx` line 358.

### API / Backend
- **Confirm Resend env vars in Vercel**: `RESEND_API_KEY` and `RESEND_AUDIENCE_ID` must both be set. The subscribe handler silently succeeds without them (`fallback: true`). Check the Vercel dashboard.
- **`generate-content.js` vs `llmFetch.js` divergence** — the Vercel API route (`api/generate-content.js`) calls Anthropic directly with `claude-sonnet-4-6`, while the frontend `llmFetch.js` uses OpenRouter. The API route is unused by the UI (the frontend calls OpenRouter). Decide which path to keep or document the dual approach.

### Site / Domain
- **Buy `sofar.cam` domain** — DEPLOY.md documents the exact steps. Cheapest option: porkbun.com (~$10-20/yr). Vercel DNS config is ready to go.
- **Add `sofar.cam` to Vercel** once domain is purchased — just 3 DNS entries.

### Content / Social
- **YouTube channel** — linked as `youtube.com/sofarcam` but no indication it has active uploads. If starting fresh, Shorts → long-form funnel is the highest-ROI first move per the site's own trend data.
- **@sofar.cam Instagram** — personal account linked. Feed it with content from the site's tools to build the loop.

---

## Blockers

- **Resend API key status unknown** — if not set in Vercel, every waitlist signup is silently dropped. No way to verify without checking the Vercel dashboard directly.
- **OpenRouter API key** (`VITE_OPENROUTER_API_KEY`) must be set as a Vercel env var for any tool to function in production. If it's missing, all AI tools fail with a client-side error.
- **Domain not purchased** — site lives at `sofarcam.vercel.app`, not `sofar.cam`. Can't run as a branded product until the domain is acquired.
- **Trend data is static** — TrendingSection says "updated weekly" but data is hardcoded. This is a trust/credibility risk as it ages.

---

## Quick Wins (< 30 min each)

- Fix the "March 2026" date in `TrendingSection.jsx` line 358 — change to current date
- Add `sofar.cam` to Vercel domains once purchased
- Verify both Resend env vars in Vercel dashboard → Settings → Environment Variables
- Update `api/generate-content.js` to use OpenRouter instead of direct Anthropic (or delete it — the frontend doesn't call it)
