# Morning Briefing — Tuesday, May 19, 2026

---

## Top 3 Priorities Today

1. **Set `RESEND_AUDIENCE_ID` env var in Vercel** — email capture is live and routing signups to Resend, but if `RESEND_AUDIENCE_ID` is unset, contacts are not being stored in an audience list (only the owner notification email fires). Verify in Resend dashboard that contacts are accumulating.
2. **Update TrendingSection data** — the footer still reads "Last update: March 2026." It's May 19. Either wire this to a real data fetch or manually refresh the trend cards and timestamp.
3. **Buy `sofar.cam` domain** — the site is live at `sofarcam.vercel.app` but DEPLOY.md calls for `sofar.cam`. Porkbun has it for ~$10–20/yr. Every day without it is a branding gap.

---

## Project Status

### sofar.cam Hub Site (SofarCam)
**Status: Live on Vercel — feature-complete for MVP**

The site is deployed and functional. Stack is React 19 + Vite 7 + Tailwind 4 + Framer Motion 12.

**What's working:**
- 5 AI tools live: Concept Generator, Hook Writer, Caption Writer, LinkedIn Writer, Content Analyzer
- Multi-model LLM cascade (11 models, 3 tiers via OpenRouter) — free tier first, paid fallback
- Session memory + chained workflow (concept → hooks → captions without re-entering form)
- Email capture wired to Resend API (`/api/subscribe`) — sends owner notification on signup
- Trending section with platform-specific data for Instagram, TikTok, YouTube, Pinterest, YouTube Shorts, Etsy
- Content Analyzer supports YouTube (oEmbed) and X/Twitter (fxtwitter API); Instagram is gracefully degraded (manual paste)
- 3D hero scene with interactive mouse tracking
- Services section with 3 offerings: AI Agent Setup ($500–1,500), AI Implementation Strategy ($500–2,000), Custom AI Website ($1,500–5,000+)
- Discovery call CTA links to `shotbyseven@gmail.com`

**Last commit:** `feat: wire email capture to real Resend API` (March 13, 2026)

---

## What Each Area Needs Next

### Email / Subscriber List
- Confirm `RESEND_AUDIENCE_ID` is set in Vercel project env vars
- Check Resend dashboard for actual subscriber count
- Plan first weekly email drop — the opt-in copy promises "weekly drops"

### AI Tools
- Trend data in `TrendingSection.jsx` is hardcoded and stale (March 2026 timestamp)
- Option A: Update the static data manually each week
- Option B: Wire to a real trends API or build an `/api/trends` serverless function
- Content Analyzer Instagram path is a dead end — users get a "paste your caption" message. Consider adding a manual text input fallback so the tool still works

### Domain / Brand
- `sofar.cam` is not yet purchased — site is on `sofarcam.vercel.app`
- Upwork and Discord URLs were recently fixed (commits `7302320`, `b814c90`)
- Verify the Upwork profile link is correct and live

### Services / Business Development
- No inbound tracking in place — discovery call goes to email only
- No analytics/conversion tracking on the site (no GA, no Plausible, nothing)
- Consider adding a simple event tracker to know which tools are being used and how often

### Codebase Health
- No tests
- No TypeScript (plain JSX throughout)
- `vercel.json` present — confirm serverless function routing is correct for all `/api/*` endpoints

---

## Blockers

- **`RESEND_AUDIENCE_ID` unset (likely):** Subscribers may not be landing in an audience list. Check Vercel env vars now.
- **Stale trend data:** TrendingSection says "March 2026" — makes the site look unmaintained. Needs a refresh before any promotion push.
- **No domain:** Can't pitch `sofar.cam` until it's actually at `sofar.cam`.
